import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  boxShadow: {
    shadowColor: '#000000',
    shadowOffset: {
      width: 0,
      height: 0,
    },
    shadowOpacity: 0.05,
    shadowRadius: 10,
    elevation: 2,
    zIndex: 1000,
  },
});
